import { NumeroPersonasPipe } from './numero-personas.pipe';

describe('NumeroPersonasPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroPersonasPipe();
    expect(pipe).toBeTruthy();
  });
});
